{{! Posts written by users (with usernames) will be dsplayed here. }}
<div>
  <p>Posted by: {{post.user.username}} on {{post.createdAt}}</p>
  <h1>{{post.title}}</h1>
  <p>{{post.content}}</p>
</div>
<div>
  <form id="comment-form">
    <label for="content">Comment as {{username}}</label>
    <textarea
      id="content"
      name="content"
      placeholder="What are your thoughts?"
    ></textarea>
    <button id="commentBtn" data-id="{{post.id}}">Comment</button>
  </form>
</div>

{{#if post.Comment.length}}
  <h3>Comments</h3>
  {{#each post.Comment}}
    <div>
      <p>{{this.content}}</p>
      <p>By: {{this.commentAuthor}}</p>
      <p> {{this.createdAt}}</p>
    </div>
  {{/each}}
{{else}}
  <p>No comments yet.</p>
{{/if}}

{{! Form to add a comment to a post. }}

<script src="/js/forum.js"></script>