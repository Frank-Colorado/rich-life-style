<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Rich Life</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />

    <link rel="stylesheet" href="/css/style.css" />
  <link href="https://fonts.googleapis.com/css2?family=Bruno+Ace&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Wix+Madefor+Display&display=swap" rel="stylesheet">

  </head>
  <body>

        <nav
      class="navbar navbar-expand-lg navbar-light"
      style="background-color: #8af2f4aa"
    >
      <div class="container-fluid p-2 navbar-font">
        <h1 style="color:#45415b; font-family: 'Bruno Ace', sans-serif;">Rich Lifestyle</h1>
      </div>
      {{#if loggedIn}}
        <div class="dropdown">
          <button
            class="btn btn-info dropdown-toggle m-3"
            type="button"
            id="dropdownBtn"
            data-toggle="dropdown"
            aria-haspopup="true"
            aria-expanded="true"
          >
            +
          </button>
          <div id="dropdown" class="show">
            <a id="homeBtn" class="dropdown-item text-light" href="/home">Home</a>
            <a id="dashboardBtn" class="dropdown-item text-light" href="/dashboard">Dashboard</a>
            <a id="logoutBtn" class="dropdown-item text-light" href="/logout">Log out</a>
          </div>
        </div>

      {{/if}}

    </nav>

    <!-- Render the sub layout -->
    {{{body}}}

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}

    <div>
      {{! We need a form here }}
      <form action=""></form>
    </div>

    <style>
      body { background-image:
      url("https://images.pexels.com/photos/19670/pexels-photo.jpg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1");
      background-repeat: no-repeat; -webkit-background-size: cover;
      -moz-background-size: cover; -o-background-size: cover; background-size:
      cover; }
    </style>

    <script src="/js/main.js"></script>

  </body>
</html>